services:
  - type: web
    name: cybix-x1
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: MAX_NEW_TOKENS
        value: "1024"
      - key: TEMPERATURE
        value: "0.75"
