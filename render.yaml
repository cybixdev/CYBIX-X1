services:
  - type: web
    name: cybix-x1
    env: docker
    dockerfilePath: ./Dockerfile
    plan: gpu-small      # A10G 6 GB VRAM – $0.65/h
    envVars:
      - key: MAX_NEW_TOKENS
        value: "1024"
      - key: TEMPERATURE
        value: "0.75"
