services:
  - type: pserv
    name: cybix-real-ai
    env: docker
    dockerfilePath: ./Dockerfile.gpu
    plan: gpu-small   # A10G 6 GB VRAM – $0.65/h
    envVars:
      - key: MAX_NEW_TOKENS
        value: "512"
      - key: TEMPERATURE
        value: "0.7"
